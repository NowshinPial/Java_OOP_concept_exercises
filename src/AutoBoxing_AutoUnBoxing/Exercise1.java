package AutoBoxing_AutoUnBoxing;

/**
 * Task: Write a program that demonstrates autoboxing by using a collection that only accepts objects. Use a list to store primitive integers and print their values.
 *
 * 1. Create an ArrayList<Integer> to store integers.
 * 2. Add three primitive int values to the list (e.g., 5, 10, 15).
 * 3. Iterate over the list using an enhanced for-loop and print each value.
 * 2. Observe how primitives are converted to wrapper objects (autoboxing).
 *
 * Expected Behavior:
 * The primitive values should be automatically boxed into Integer objects when added to the list.
 */
public class Exercise1 {
}
