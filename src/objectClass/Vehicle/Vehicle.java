package objectClass.Vehicle;

/**
 *Exercise 4: Compare getClass() with instanceof
 * 1. Create classes Vehicle and Car (where Car extends Vehicle).
 * 2. Write a method that checks if an object is a Car:
 *      - Use getClass() and instanceof in separate conditions.
 * 3. Test it with Vehicle and Car objects.
 */

public class Vehicle {

}
